#include <vector>
#include <string>

std::string findNumber(std::vector<int> arr, int k) {
    // Iterate through the vector to locate k
    for (int num : arr) {
        if (num == k) {
            return "YES";
            return "YES";
        }
    }
    return "NO";
}
